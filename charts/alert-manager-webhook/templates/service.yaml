apiVersion: v1
kind: Service
metadata:
  name: sd-alert-webhook
  labels:
    app: sd-alert-webhook
spec:
  type: ClusterIP
  ports:
  - port: 8080
    name: alert-webhook
    protocol: TCP
    targetPort: http
  selector:
    app: sd-alert-webhook